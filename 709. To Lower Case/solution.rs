impl Solution {
    pub fn to_lower_case(str: String) -> String {
        // Sorta cheating but whatever it works.
        // Lazy solution = better solution.
        let lower = str.to_lowercase();
        return lower;
    }
}
